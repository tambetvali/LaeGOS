Lanes generation was heavily associated with this project: I needed correct numbers with at least basic level of images so that I have somewhere to go on from:
- [20MB database of Laegna lanes].

In this database, there are:
- R=0.5: binary True / False => A / O respectively. This is both binary projection (maps to fractal - IO <=> AE opposition, IA <=> OE opposition, and consequentively their diagonal if values are added, and two identic logic models work on both frequencies - summed frequency with dimensionality and thus length reduction by two, gives on diagonal, the same logic system third time. Proof: if two channels are given same values and randomization results, the diagonal would show a single model of that type with single input.
- R=1: base-4 Negotion (I), Negation (O), Position (A) and Posetion (E). Abstraction: for example, model of good and bad can be fractal-projected here, resolved into multifrequency solution integration it's own views implied, and reduced back down in dimensionality to give the original binary system: which *yet* contains long-term logic, which can be correctly projected as two bands are given, the logic responds differently to values on which band it is - this is desired as indeed, the logecs system now enters with it's full R-T consideration of two bits (double-binary, binary-binary, with in yang and tao - the diagonal, cross-production, is impled). R bit is "space", resolves if the meaning is non-locally different, for example the outcome is different or what it really means or does.
- R=2: 2-digit base-4; it's one-digit folding in infinity and if second digit (then T) is local context, and first digit (then R) is it's infinity resolution, base-4 2-digit Laegna is simplest projection of full infinities, model complexity easily reduced meaningfully, but automatically, like reducing dimensions in Hilbert's spaces.
- R=3: 3-digit base-4, one I am interested in less, but basically it projects to 1/2 *from direction of infinity* and has been a good playground of some math, as I implicitly use O as 1/2 in my custom affine translation.
- R=4: 4-digit base is complexity of real infinity models.
  - R=16: 16-digit base, especially with complex numbers which have 16 value space, maps to this by diagonal.
    - To the end, where we have 0.5 digits, we get diagonals: after that, without raising the resolution which leads us away from pure 0.5 and needs resolution of our intent and resolution, which is possible and could be automatic, but directly R=0.5, at diagonal identity, maps to R=0.5, by which the base identity is a binary linear system, while where it enters, it's a fractal hologram.
   
Theorem in Laegna: number is naturally a fractal hologram. In number digit positioning, it's not otherwise possible.
- It's not necessarily physical, logical or systematic or mental fractal hologram, but adapts hologram fractal logic by user's units.
  - It's not distributed to domain of belief, and spiritual, material models and math models are given in Laegna units, but I explicitly state it's less a place to decide model validity: rather, we decide some common harmonic parameters.

We have now Laegna Lanes in all 4 dimensions:
- Graphics should be *numerically correct*, while for each other file it's perfectly fine.
  - It could be more scientific to use tiff with metadata, and precisely describe what is meant. This is used in geographical and scientific data, and the question is: does it enable describing any kind of coordinate systems?
  - Another, better yet applicable model for 2D graphics is Povray. It's raytrace 3D system with literal description.

